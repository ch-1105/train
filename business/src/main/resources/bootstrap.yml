spring:
  application:
    name: business
  profiles:
    active: dev # 开发环境
  cloud:
    nacos:
      # 配置中心
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        namespace: train
      # 注册中心
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: train